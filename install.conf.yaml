- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.zsh: zsh
    ~/.config/nvim: nvim
    ~/.oh-my-zsh: .oh-my-zsh/
    ~/.gitignore: gitignore  

- shell:
  - [git submodule update --init --recursive, Installing submodules]